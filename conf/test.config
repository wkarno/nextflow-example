/*
========================================================================================
    Test Configuration
========================================================================================
    Reduced resources for testing with small datasets
*/

params {
    // Reduced resource limits for testing
    max_cpus = 2
    max_memory = '4.GB'
    max_time = '1.h'
}
